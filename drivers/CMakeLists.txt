set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR})
#add_subdirectory(tests)
add_subdirectory(microsd)
target_sources(${PROJECT_NAME} PUBLIC )
target_include_directories(${PROJECT_NAME} PUBLIC .)
